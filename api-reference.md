# API Reference

## Authentication

Use the `authenticate(api_key, timeout=30)` function to authenticate your session.

## Functions

### start_audit(api_key)

Starts the documentation audit process.
